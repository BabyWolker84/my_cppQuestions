Что такое статическое связывание (static binding) и как оно работает в C++?

Ответ: 
В чем отличие статического связывания от динамического связывания?

Ответ: 
Как в C++ определить виртуальную функцию и зачем они нужны?

Ответ: 
Что происходит при переопределении виртуальных функций в производных классах?

Ответ: 
Каким образом динамическое связывание решает, какую функцию вызвать?

Ответ: 
Какой модификатор используется для объявления виртуальной функции в базовом классе?

Ответ: 
Какие классы называются полиморфными в C++?

Ответ: 
Можно ли вызывать виртуальные функции через имя объекта статически?

Ответ:
Каким образом осуществляется динамическое связывание в C++?

Ответ: 
Какие ограничения существуют при определении виртуальных функций в производных классах?

Ответ: 
Что делает ключевое слово override при переопределении виртуальных функций?

Ответ: 
Какие ошибки могут возникнуть при неправильном использовании ключевого слова override?

Ответ: 
Зачем использовать ключевое слово override при переопределении виртуальных функций?

Ответ: 
Какие проблемы могут возникнуть, если не использовать ключевое слово override при переопределении виртуальных функций?

Ответ: 
Можно ли использовать ключевое слово override для не виртуальных функций?

Ответ: 
Какие преимущества и недостатки связаны с использованием виртуальных функций?

Ответ: 
Что такое виртуальная таблица (vtable) и как она связана с виртуальными функциями?

Ответ: 
Какие накладные расходы связаны с вызовом виртуальных функций?

Ответ: 
Ключевое слово которое означает что класс не наследуется?

Ответ: 