Каким образом можно инициализировать вектор?

Ответ: std::vector<тип_Данных> название_вектора = {значение, значение, значение . . .}; std::vector<тип_Данных> название_вектора(Количество_элементов, значение_элементов);
Какие способы обращения к элементам вектора существуют?

Ответ: 
Чем отличаются круглые и фигурные скобки при инициализации вектора?

Ответ: 
Можно ли хранить в векторе элементы разных типов?

Ответ: 
Как получить первый и последний элементы вектора?

Ответ: 
Как получить элемент вектора по индексу?

Ответ: 
Что произойдет при попытке обращения к элементу вектора по недопустимому индексу?

Ответ: 
Как изменить значение элемента вектора?

Ответ: 
Что произойдет при попытке добавить элемент в вектор по индексу, превышающему его размер?

Ответ: 
Какой тип исключения будет сгенерирован при попытке обращения к элементу вектора с использованием функции at(), если индекс недопустим?

Ответ: 
Что произойдет в приведенном примере программы, если индекс, переданный функции at(), выходит за пределы размера вектора?

Ответ: 
Когда использовать вектор?

Ответ: 